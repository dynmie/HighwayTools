## Git

- Never commit unless explicitly told to.
- Never push unless explicitly told to.
- Do NOT add `Co-Authored-By` trailers to commit messages.
- Do NOT state in any way that code was generated by an agent. Responsibility lies in the developer, not the agent.

## Commands

- `./gradlew build` — compile and package the mod jar (output: `build/libs/highway-<version>.jar`).
- `./gradlew compileJava` — fast compile check only.
- `./gradlew runClient` — launch Minecraft with the addon loaded (development game dir is `run/`).
- `./gradlew processResources` — expand `${version}`/`${mc_version}` placeholders into `fabric.mod.json`.
- There are no unit tests in this project; `./gradlew build` is the primary verification.

## Stack (verified current)

- Minecraft **26.1.2** (first **unobfuscated** release) — Mojang official names ARE the class names; no yarn/mojmap mappings are declared. So e.g. `net.minecraft.core.BlockPos`, `net.minecraft.world.phys.Vec3`, `net.minecraft.world.level.block.Block`, `net.minecraft.network.chat.Component`.
- meteor-client **26.1.2-SNAPSHOT**, baritone **26.1-SNAPSHOT** (mod id `baritone-meteor`; nether-pathfinder is bundled inside it), fabric-loader **0.19.3**.
- Loom plugin **`net.fabricmc.fabric-loom`** (non-remap): no `mappings`, no `modImplementation`/`modCompileOnly`, no `remapJar` — use plain `implementation`.
- JDK **25** toolchain (auto-provisioned by the foojay resolver in `settings.gradle.kts`); Gradle wrapper **9.6.1**.
- The two dev mods (meteor-client, baritone) come from `https://maven.meteordev.org` (releases + snapshots).

**Lambda reference (important):** the storage/restock/placement/grind systems are faithful ports of **Lambda HighwayTools** (Kotlin, MC 1.12.2) at `C:\Users\misaka\IdeaProjects\LHighwayTools`. Ground truth lives there: `src/main/kotlin/trombone/handler/Container.kt` + `Inventory.kt` (storage), `trombone/task/TaskExecutor.kt` + `TaskManager.kt` (container lifecycle, grind chaining), `trombone/place/` (placement search). When behavior seems wrong, **check the Lambda source before "fixing"** — the port deliberately mirrors it, including quirks. Meteor sources (for `ItemListSetting` etc.) are in the gradle cache at `.../meteor-client-26.1.2-SNAPSHOT-sources.jar`.

## Architecture

This is a **Meteor Client addon** (a Fabric client mod). Entry point is `me.dynmie.highway.HighwayAddon` (`MeteorAddon`), registered via the `"meteor"` entrypoint in `fabric.mod.json`. On init it registers:
- the `HighwayTools` module (`modules/HighwayTools.java`) under a custom `Category`,
- a `BaritoneProcess` with Baritone's pathing control manager,
- a `CheckBlocksCommand` (lists current block tasks). Ignore-list and distance are GUI settings on the module, not commands.

**Core loop** — the `HighwayTools` module is the coordinator. Each tick (`TickEvent.Pre`) it runs the following pipeline:

1. `BlueprintGenerator` produces a `Map<BlockPos, BlueprintTask>` — the desired state of the highway. A `BlueprintProvider` (`StraightBlueprintProvider` or `DiagonalBlueprintProvider`, chosen by direction) supplies the front/floor/railings/above-railings positions.
2. `BlockTaskManager` converts the blueprint into `BlockTask`s by comparing the world state to the target block at each position, and holds them in a `ConcurrentHashMap`.
3. `TaskExecutor` drives each `BlockTask` through the `TaskState` state machine (BREAK → BREAKING → BROKEN → PLACE → PLACED → DONE, plus LIQUID / PENDING_*), delegating actual work to the handlers.
4. Handlers: `BreakHandler` (packet mining — START/STOP `ServerboundPlayerActionPacket` sent through `startPrediction`), `PlaceHandler` (placement via meteor's `BlockUtils.interact` against a `PlacementStep`), `InventoryHandler` (item/tool finding + hotbar movement, using meteor's `InvUtils`/`Utils.getEnchantmentLevel`), `LiquidHandler` (spawns fill tasks for adjacent liquids).

**Placement** (`highwaytools/place/`): `PlacementSearcher` (ported from Lambda's `getNeighbourSequence`) finds a chain of `PlacementStep`s (support block + click side + hit vector) up to `placement-search` depth. A task is only placed when a sequence exists; the deep search lets the bot place into gaps with no direct support.

**Storage / restock** (`highwaytools/container/` + `highwaytools/handler/InventoryManager.java`) — a faithful port of Lambda's `Container`/`Inventory`/`TaskExecutor` restock system. When material or tools run low (`needsRestock()`), `BlockTaskManager.startRestock` picks a source by priority: ender-chest grind (AutoObsidian) → a shulker box holding the item → the player's ender chest. The active container runs as a `ContainerTask` inside the per-tick loop (`runTasks` container branch) with its own lifecycle `PLACE → OPEN_CONTAINER → RESTOCK → BREAK → PICKUP → DONE`; while it's non-null the pathfinder is in RESTOCK/PICKUP state and the normal block loop keeps ticking (update-only) so nothing freezes.
- `ContainerTask` holds the container block (`containerBlock`), whether it's a shulker (`isShulker()` → drops itself) or ender chest (drops 8 obsidian → `dropItem()`), and a `destroy` flag meaning "break right after placing WITHOUT restocking" (AutoObsidian grind only — every other container is still broken after restock via RESTOCK→BREAK).
- **Grind chaining** (`grindCycles`): while `grindCycles > 0` and the material is obsidian, `needsRestockCheck()` spawns consecutive ender-chest grinds (Lambda's `TaskManager` branch) so obsidian accumulates to fill the inventory before building resumes. Budget = `freeSlots() * 8`. Mid-chain grinds set `collect=false` (obsidian piles left on the ground); the final grind's PICKUP collects all piles because `getCollectingPosition` searches within range of the **player**, not the container.
- **Restock pull** (`TaskExecutor.pullOneStack`) mirrors Lambda's `moveToInventory`: QUICK_MOVE into a mergeable partial, else SWAP into an empty hotbar slot (only falling back to an eject-list trash slot when nothing empty remains), else a 2-click PICKUP carry into an empty main slot. Each click is a single `ServerboundContainerClickPacket` acked by the server `stateId` advancing (`clickQueueBusy` + `lastClickStateId` gate one click per tick — clicks cannot be batched).
- **Trash / eject** is a user-configurable `eject-list` setting (Lambda's `defaultEjectList`). ONLY listed items are ever swapped away or dropped to make room — a kept item (flint & steel, golden apple, tool) is preserved even when full. `freeSlots()` counts only genuinely empty slots (reserving one for the container pickup + the keep-free margin), so ground litter can't occupy the slot the shulker box needs.
- **Robustness:** a `ContainerTask` stuck past its state's `stuckTimeout` aborts to DONE (Lambda parity) so an unreachable pickup or a full inventory of kept items can't stall the module forever.

**Mining** — the packet-based path (startPrediction + START/STOP) is what actually breaks blocks; the `MultiPlayerGameModeAccessor` mixin exposes `startPrediction` for this. The insta/creative path sends START and waits for the server ack via the PENDING_BREAK state.

**Baritone integration** (in `highwaytools/pathing/`): `BaritoneHelper` saves/restores baritone settings on module activate/deactivate; `BaritonePathfinder` advances the goal position along the highway direction; `BaritoneProcess` (an `IBaritoneProcess`) paths to that goal.

**Utils** (`utils/`): small static helpers (`BlockUtils`, `LiquidUtils`, `LocationUtils`, `DirectionUtils`).

## Important

- `run/` is the development Minecraft game directory (world saves, meteor config/profiles/waypoints, baritone settings). It contains real user data — **never delete or restructure it**.
- **The bot controls the hotbar — never `swapBack`.** `prepareItemInHotbar`/`prepareToolInHotbar` return the hotbar slot that actually holds the item, and the caller `InvUtils.swap(slot, false)` selects it. Restoring the previous slot was the cause of a hotbar/placement desync bug.
- **`waitTicks` is placement pacing only.** It's a shared counter decremented once per tick in `runTasks()`; the per-task gate holds back only PLACE/PLACED tasks while it's > 0. The break path must never set it — doing so throttles every break (the cause of a "mining is super slow" bug).
- **Container clicks are one-per-tick and ack-gated.** `ServerboundContainerClickPacket` carries the menu `stateId`; the client only advances `stateId` on the server ack, so a second click sent before the ack carries a stale stateId and changedSlots the server hasn't confirmed. Never batch clicks — the `clickQueueBusy`/`lastClickStateId` gate exists for this. Also never send a container click while the container menu isn't open (`containerId == 0`).
- When migrating or writing MC code, use mojmap names (see Stack). Meteor 26.1 helpers worth reusing instead of re-implementing: `meteordevelopment.meteorclient.utils.world.BlockUtils` (`interact`, `canPlace`, `getDirection`, `breakBlock`), `meteordevelopment.meteorclient.utils.player.InvUtils` (`find`, `move`, `swap`, `testInMainHand`), `meteordevelopment.meteorclient.utils.Utils.getEnchantmentLevel`. Note: this MC version has **no `PickaxeItem`/`DiggerItem` class** — detect pickaxes via the `Tool` component (`stack.get(DataComponents.TOOL)` + `isCorrectForDrops`).
